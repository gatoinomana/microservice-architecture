type Todo {
  studentId: String!
}

type Query {
  allTodosStudent(studentId: String!): [Todo]
}

type Mutation {
  createTodo(studentId: String!): Todo
}

schema {
  query: Query
  mutation: Mutation
}